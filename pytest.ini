[pytest]
# プロジェクトルートを import パスに含め、`pytest` 単体実行でも `modules` を解決する（pytest 7+）
pythonpath = .
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
